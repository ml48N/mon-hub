<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Hub Divertissement - Jeux, Animes & Musique</title>
    <style>
        /* --- Variables de thème --- */
        :root {
            --bg-color: #121212;
            --card-bg: #1e1e1e;
            --text-color: #e0e0e0;
            --primary-color: #6200ea;
            --accent-color: #03dac6;
            --border-color: #333;
        }

        /* --- Thème Clair --- */
        body.light-theme {
            --bg-color: #f5f5f5;
            --card-bg: #ffffff;
            --text-color: #333333;
            --primary-color: #3700b3;
            --accent-color: #018786;
            --border-color: #ddd;
        }

        /* --- Style général --- */
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            margin: 0;
            padding: 0;
            transition: background-color 0.3s, color 0.3s;
        }

        /* --- Header et Bouton Thème --- */
        .header {
            background-color: var(--card-bg);
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid var(--border-color);
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .header h1 {
            margin: 0;
            color: var(--primary-color);
        }
        .theme-switch-container {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .theme-switch {
            position: relative;
            display: inline-block;
            width: 60px;
            height: 34px;
        }
        .theme-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }
        .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #ccc;
            transition: .4s;
            border-radius: 34px;
        }
        .slider:before {
            position: absolute;
            content: "🌙";
            height: 26px;
            width: 26px;
            left: 4px;
            bottom: 4px;
            background-color: white;
            transition: .4s;
            border-radius: 50%;
            text-align: center;
            line-height: 26px;
        }
        input:checked + .slider {
            background-color: var(--primary-color);
        }
        input:checked + .slider:before {
            transform: translateX(26px);
            content: "☀️";
        }

        /* --- Menu de Navigation --- */
        .nav-menu {
            background-color: var(--card-bg);
            padding: 0;
            list-style: none;
            display: flex;
            justify-content: center;
            margin: 0;
            border-bottom: 1px solid var(--border-color);
        }
        .nav-menu li {
            margin: 0;
        }
        .nav-menu a {
            display: block;
            color: var(--text-color);
            text-decoration: none;
            padding: 15px 25px;
            font-weight: bold;
            transition: background-color 0.3s, color 0.3s;
        }
        .nav-menu a:hover, .nav-menu a.active {
            background-color: var(--primary-color);
            color: white;
        }

        /* --- Contenu des Sections --- */
        .content-section {
            display: none; /* Caché par défaut */
            padding: 30px;
            min-height: 80vh;
        }
        .content-section.active {
            display: block; /* Affiché quand actif */
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        /* --- Styles pour les iframes --- */
        .iframe-container {
            width: 100%;
            height: 80vh; /* Prend presque toute la hauteur de l'écran */
            border: 1px solid var(--border-color);
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
        }
        .iframe-container iframe {
            width: 100%;
            height: 100%;
            border: none;
            border-radius: 8px;
        }

        /* --- Message d'avertissement --- */
        .warning-message {
            background-color: #f9a825;
            color: #333;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
        }
        .light-theme .warning-message {
            background-color: #ffca28;
        }
    </style>
</head>
<body>

    <header class="header">
        <h1>🎮 Mon Hub Divertissement 🎬</h1>
        <div class="theme-switch-container">
            <span>Dark</span>
            <label class="theme-switch">
                <input type="checkbox" id="themeToggle">
                <span class="slider"></span>
            </label>
            <span>Light</span>
        </div>
    </header>

    <nav>
        <ul class="nav-menu">
            <li><a href="#" class="nav-link active" data-target="jeux">🎮 Jeux (Snokido)</a></li>
            <li><a href="#" class="nav-link" data-target="animes">🎬 Animes (Anime-Sama)</a></li>
            <li><a href="#" class="nav-link" data-target="musique">🎵 Musique (Spotify)</a></li>
        </ul>
    </nav>

    <main>
        <!-- Section Jeux -->
        <section id="jeux" class="content-section active">
            <div class="container">
                <h2>Jouer aux jeux de Snokido</h2>
                <div class="warning-message">
                    ⚠️ Vous êtes sur la version officielle de Snokido. Choisissez votre jeu et amusez-vous bien !
                </div>
                <div class="iframe-container">
                    <iframe src="https://www.snokido.fr/" title="Jeux Snokido"></iframe>
                </div>
            </div>
        </section>

        <!-- Section Animes -->
        <section id="animes" class="content-section">
            <div class="container">
                <h2>Regarder les Animes sur Anime-Sama</h2>
                <div class="warning-message">
                    ⚠️ Vous êtes sur la version officielle d'Anime-Sama. Cliquez sur l'anime de votre choix pour le streaming.
                </div>
                <div class="iframe-container">
                    <iframe src="https://www.anime-sama.fr/" title="Animes Anime-Sama"></iframe>
                </div>
            </div>
        </section>

        <!-- Section Musique -->
        <section id="musique" class="content-section">
            <div class="container">
                <h2>Écouter la musique sur Spotify</h2>
                <div class="warning-message">
                    ⚠️ Vous utilisez le lecteur web de Spotify. Connectez-vous pour accéder à toute votre bibliothèque.
                </div>
                <div class="iframe-container">
                    <iframe src="https://open.spotify.com/" title="Musique Spotify"></iframe>
                </div>
            </div>
        </section>
    </main>

    <script>
        // --- Logique du menu de navigation ---
        const navLinks = document.querySelectorAll('.nav-link');
        const sections = document.querySelectorAll('.content-section');

        navLinks.forEach(link => {
            link.addEventListener('click', (event) => {
                event.preventDefault(); // Empêche le lien de recharger la page

                const targetId = link.getAttribute('data-target');

                // Retirer la classe 'active' de tous les liens et sections
                navLinks.forEach(l => l.classList.remove('active'));
                sections.forEach(s => s.classList.remove('active'));

                // Ajouter la classe 'active' au lien cliqué et à
                // Ajouter la classe 'active' au lien cliqué et à la section correspondante
                link.classList.add('active');
                document.getElementById(targetId).classList.add('active');
            });
        });

        // --- Logique du bouton Light/Dark ---
        const themeToggle = document.getElementById('themeToggle');
        const body = document.body;

        // Vérifier si un thème est déjà enregistré dans le navigateur
        const currentTheme = localStorage.getItem('theme');
        if (currentTheme === 'light') {
            body.classList.add('light-theme');
            themeToggle.checked = true; // Met le bouton sur la position "clair"
        }

        // Écouter le clic sur le bouton pour changer le thème
        themeToggle.addEventListener('change', () => {
            if (themeToggle.checked) {
                body.classList.add('light-theme');
                localStorage.setItem('theme', 'light'); // Enregistre le choix
            } else {
                body.classList.remove('light-theme');
                localStorage.setItem('theme', 'dark'); // Enregistre le choix
            }
        });
    </script>
</body>
</html>